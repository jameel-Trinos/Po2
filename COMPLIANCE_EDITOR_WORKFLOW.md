# Compliance Editor - Visual Workflow

## Page Layout

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Financial Compliance Editor                    [Balance: $100.00] 💰  │
│  Upload documents for FINRA/SEC compliance review                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│                    ┌──────────────────────────┐                        │
│                    │   📄 Upload Document     │                        │
│                    │                          │                        │
│                    │  Select Word Document    │                        │
│                    │  [Choose File] [Analyze] │                        │
│                    │                          │                        │
│                    │  Supported: .docx only   │                        │
│                    └──────────────────────────┘                        │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

## After Upload - Split View

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Financial Compliance Editor                    [Balance: $99.90] 💰   │
│  📊 5 Total | ✅ 0 Applied | 🔴 2 Critical                              │
│  [Download Word] [Download PDF] [New Document]                          │
├──────────────────────────────────────┬──────────────────────────────────┤
│                                      │                                   │
│  📝 Document Editor                  │  📋 Compliance Suggestions (5)   │
│  ┌────────────────────────────────┐ │  ┌─────────────────────────────┐│
│  │                                │ │  │ 🟣 FINRA ─────────── 2 pending││
│  │  TinyMCE Rich Text Editor      │ │  │                              ││
│  │                                │ │  │ ┌─────────────────────────┐ ││
│  │  Your document content with    │ │  │ │ 🔴 critical  [FINRA]    │ ││
│  │  full editing capabilities...  │ │  │ │                         │ ││
│  │                                │ │  │ │ Issue: FINRA Rule 2210  │ ││
│  │  Highlighted text shows when   │ │  │ │ prohibits guarantees... │ ││
│  │  you click a suggestion →      │ │  │ │                         │ ││
│  │                                │ │  │ │ ❌ "guaranteed returns" │ ││
│  │  [Formatting toolbar at top]   │ │  │ │ ✅ "potential returns"  │ ││
│  │  Bold | Italic | Lists | etc   │ │  │ │                         │ ││
│  │                                │ │  │ │    [Apply Change]       │ ││
│  │                                │ │  │ └─────────────────────────┘ ││
│  │                                │ │  │                              ││
│  │                                │ │  │ 🔵 SEC ──────────── 1 pending││
│  │                                │ │  │                              ││
│  │  [Auto-save enabled]           │ │  │ ┌─────────────────────────┐ ││
│  │                                │ │  │ │ ⚠️ warning   [SEC]      │ ││
│  │                                │ │  │ │ ...                     │ ││
│  │                                │ │  │ └─────────────────────────┘ ││
│  │                                │ │  │                              ││
│  └────────────────────────────────┘ │  │ 🟢 Grammar ─────── 2 pending ││
│                                      │  │                              ││
│                                      │  │ [Scrollable list...]         ││
│                                      │  └─────────────────────────────┘│
└──────────────────────────────────────┴──────────────────────────────────┘
```

## User Interaction Flow

```
1. UPLOAD
   ┌──────────┐
   │ Click    │
   │ Upload   │──→ Select .docx file
   └──────────┘
        │
        ↓
   ┌──────────┐
   │ Analyze  │──→ POST /api/compliance/analyze
   └──────────┘
        │
        ↓
   ┌──────────────────────────────┐
   │ Server Processing:           │
   │ 1. Extract text (mammoth)    │
   │ 2. Convert to HTML           │
   │ 3. Generate suggestions      │
   │ 4. Return analysis           │
   └──────────────────────────────┘
        │
        ↓
   ┌──────────────────────────────┐
   │ UI Updates:                  │
   │ • Show split view            │
   │ • Load editor with content   │
   │ • Display suggestions        │
   │ • Deduct $0.10 from balance  │
   └──────────────────────────────┘

2. REVIEW SUGGESTIONS
   ┌──────────────┐
   │ Click        │
   │ Suggestion   │──→ Highlights text in editor
   └──────────────┘       Scrolls to location
        │
        ↓
   ┌──────────────────────────────┐
   │ Suggestion Card Selected:    │
   │ • Green border               │
   │ • Text highlighted in editor │
   │ • "Apply" button visible     │
   └──────────────────────────────┘

3. APPLY CHANGE
   ┌──────────────┐
   │ Click Apply  │──→ POST /api/compliance/apply-change
   └──────────────┘
        │
        ↓
   ┌──────────────────────────────┐
   │ Server Processing:           │
   │ 1. Validate request          │
   │ 2. Mock DB update            │
   │ 3. Return success            │
   └──────────────────────────────┘
        │
        ↓
   ┌──────────────────────────────┐
   │ UI Updates:                  │
   │ • Replace text in editor     │
   │ • Mark suggestion as applied │
   │ • Show ✅ on card            │
   │ • Deduct $0.01 from balance  │
   │ • Toast notification         │
   └──────────────────────────────┘

4. EXPORT
   ┌──────────────────┐
   │ Click Download   │
   │ [Word] or [PDF]  │
   └──────────────────┘
        │
        ├─→ Word: Convert HTML to .docx (docx library)
        │         Download file
        │
        └─→ PDF:  Convert HTML to PDF (pdf-lib)
                  Download file
```

## Suggestion Card States

```
PENDING (Not Applied)
┌─────────────────────────────────┐
│ 🔴 critical        [FINRA]      │
│                                 │
│ FINRA Rule 2210 prohibits...   │
│                                 │
│ ┌─────────────────────────────┐│
│ │ ❌ Original:                ││
│ │ "guaranteed returns"        ││
│ └─────────────────────────────┘│
│ ┌─────────────────────────────┐│
│ │ ✅ Suggested:               ││
│ │ "potential returns"         ││
│ └─────────────────────────────┘│
│                                 │
│        [Apply Change]           │
└─────────────────────────────────┘

APPLYING (Processing)
┌─────────────────────────────────┐
│ 🔴 critical        [FINRA]      │
│                                 │
│ FINRA Rule 2210 prohibits...   │
│                                 │
│ ❌ "guaranteed returns"         │
│ ✅ "potential returns"          │
│                                 │
│        [Applying...]  🔄        │
└─────────────────────────────────┘

APPLIED (Completed)
┌─────────────────────────────────┐
│ ✅ Applied                       │
└─────────────────────────────────┘
```

## Balance Indicator States

```
HEALTHY ($50+)
┌──────────────────────────┐
│ 💰 Available Credits     │
│ $100.00         [Healthy]│
└──────────────────────────┘
        (Green)

LOW ($20-49)
┌──────────────────────────┐
│ 💰 Available Credits     │
│ $35.00            [Low]  │
└──────────────────────────┘
        (Yellow)

CRITICAL (<$20)
┌──────────────────────────┐
│ 💰 Available Credits     │
│ $5.00         [Critical] │
└──────────────────────────┘
        (Red)
```

## Data Flow Architecture

```
┌─────────────────┐
│   User Browser  │
└────────┬────────┘
         │
         │ 1. Upload .docx
         ↓
┌─────────────────────────────────────────┐
│   Frontend: compliance-editor/page.tsx  │
│   • DocumentUpload component            │
│   • FormData creation                   │
└────────┬────────────────────────────────┘
         │
         │ 2. POST /api/compliance/analyze
         ↓
┌─────────────────────────────────────────┐
│   API: /api/compliance/analyze/route.ts │
│   • Validate file type                  │
│   • Extract text (mammoth)              │
│   • Convert to HTML                     │
│   • Run compliance checks               │
│   • Generate suggestions                │
└────────┬────────────────────────────────┘
         │
         │ 3. Return analysis results
         ↓
┌─────────────────────────────────────────┐
│   Frontend: Split View                  │
│   • TinyMCEEditor (left)                │
│   • SuggestionCard list (right)         │
│   • BalanceIndicator (top)              │
└────────┬────────────────────────────────┘
         │
         │ 4. User clicks "Apply"
         ↓
┌──────────────────────────────────────────┐
│   API: /api/compliance/apply-change      │
│   • Validate change                      │
│   • Mock DB update                       │
│   • Return success                       │
└────────┬─────────────────────────────────┘
         │
         │ 5. Update UI
         ↓
┌─────────────────────────────────────────┐
│   Frontend: Apply Change                │
│   • Replace text in editor              │
│   • Mark suggestion as applied          │
│   • Update balance                      │
│   • Show toast notification             │
└─────────────────────────────────────────┘
```

## Database Schema Relationships

```
┌──────────────┐
│    users     │
│──────────────│
│ id (PK)      │
│ name         │
│ email        │
│ balance      │─┐
└──────────────┘ │
                 │
                 │ userId (FK)
                 │
                 ↓
           ┌──────────────┐
           │  documents   │
           │──────────────│
           │ id (PK)      │─┐
           │ userId       │ │
           │ title        │ │
           │ content      │ │
           │ status       │ │
           └──────────────┘ │
                            │
                            │ documentId (FK)
                            │
                            ↓
                      ┌──────────────┐
                      │ suggestions  │
                      │──────────────│
                      │ id (PK)      │
                      │ documentId   │
                      │ category     │
                      │ severity     │
                      │ originalText │
                      │ suggestedText│
                      │ isApplied    │
                      └──────────────┘
```

## Component Hierarchy

```
ComplianceEditorPage
├── BalanceIndicator
│   ├── Card
│   │   └── CardContent
│   ├── Badge (status)
│   └── DollarSign icon
│
├── DocumentUpload (when showUpload=true)
│   ├── Card
│   │   ├── CardHeader
│   │   └── CardContent
│   ├── Input (file)
│   ├── Button (upload)
│   └── Alert (errors)
│
└── Split View (when document loaded)
    ├── Stats Bar
    │   ├── Badge[] (stats)
    │   └── Button[] (actions)
    │
    ├── Left Panel (2/3 width)
    │   └── Card
    │       └── TinyMCEEditor
    │
    └── Right Panel (1/3 width)
        └── Card
            └── Suggestions Panel
                └── For each category:
                    ├── Badge (category name)
                    ├── Separator
                    └── SuggestionCard[]
                        ├── Badge (severity)
                        ├── Badge (category)
                        ├── Text displays
                        └── Button (apply)
```

## Cost Tracking

```
Operation                Cost      Balance Change
─────────────────────────────────────────────────
Initial Balance          —         $100.00
Document Analysis        $0.10     $99.90
Apply Suggestion 1       $0.01     $99.89
Apply Suggestion 2       $0.01     $99.88
Apply Suggestion 3       $0.01     $99.87
─────────────────────────────────────────────────
New Analysis             $0.10     $99.77
Apply Suggestion 1       $0.01     $99.76
...
```

## Mock vs Production

```
┌─────────────────────┬──────────────────┬──────────────────┐
│ Feature             │ Mock (Current)   │ Production       │
├─────────────────────┼──────────────────┼──────────────────┤
│ Document Storage    │ State only       │ Database         │
│ User Balance        │ State only       │ Database         │
│ Compliance Rules    │ Regex patterns   │ AI (Gemini)      │
│ Document ID         │ Timestamp        │ Auto-increment   │
│ Cost Deduction      │ UI state         │ DB transaction   │
│ Suggestions         │ Pattern matching │ AI analysis      │
└─────────────────────┴──────────────────┴──────────────────┘
```

---

**This workflow demonstrates the complete user journey from upload to export!**

